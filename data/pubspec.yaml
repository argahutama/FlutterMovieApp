name: data
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+3
  http: ^1.1.0
  domain:
    path: ../domain
  common:
    path: ../common

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.6.1
  injectable_generator: ^2.1.5
  build_runner: ^2.3.3
  mockito: ^5.0.8

flutter:
